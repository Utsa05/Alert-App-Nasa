import 'package:flutter/material.dart';

class LocationGPSPage extends StatelessWidget {
  const LocationGPSPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
